import pandas as pd
import sqlalchemy
import requests

print("✅ Docker + Python environment is ready!")
print("🧪 Pandas version:", pd.__version__)
print("🧪 SQLAlchemy version:", sqlalchemy.__version__)
